<Window x:Class="CarManagement.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Car Management" Height="450" Width="800">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <DataGrid x:Name="dgCars" Grid.Row="0" AutoGenerateColumns="False" IsReadOnly="True">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="50"/>
                <DataGridTextColumn Header="Name" Binding="{Binding Name}" Width="200"/>
                <DataGridTextColumn Header="Configuration" Binding="{Binding Configuration}" Width="200"/>
                <DataGridTextColumn Header="Price" Binding="{Binding Price}" Width="100"/>
                <DataGridCheckBoxColumn Header="Is Electric" Binding="{Binding IsElectric}" Width="100"/>
            </DataGrid.Columns>
        </DataGrid>

        <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Center" Margin="5">
            <Button Content="Reload" Width="100" Margin="5" Click="Reload_Click"/>
            <Button Content="Add" Width="100" Margin="5" Click="Add_Click"/>
            <Button Content="Edit" Width="100" Margin="5" Click="Edit_Click"/>
            <Button Content="Delete" Width="100" Margin="5" Click="Delete_Click"/>
        </StackPanel>
    </Grid>
</Window>
