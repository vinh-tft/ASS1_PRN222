<Window x:Class="CarManagement.CustomerView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:CarManagement"
        mc:Ignorable="d"
        Title="Customer Management" Height="500" Width="900">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Menu for CRUD operations -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" HorizontalAlignment="Left" Margin="0 0 0 10">
            <Button Content="Add Customer" Width="120" Margin="5" Click="BtnAdd_Click"/>
            <Button Content="Update Customer" Width="120" Margin="5" Click="BtnUpdate_Click"/>
            <Button Content="Delete Customer" Width="120" Margin="5" Click="BtnDelete_Click"/>
            <Button Content="Refresh" Width="80" Margin="5" Click="BtnView_Click"/>
        </StackPanel>

        <!-- Input Form -->
        <Grid Grid.Row="1" Margin="0 10 0 20">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="200"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="200"/>
            </Grid.ColumnDefinitions>

            <Label Content="ID:" Grid.Row="0" Grid.Column="0" VerticalAlignment="Center"/>
            <TextBox x:Name="txtId" Grid.Row="0" Grid.Column="1" Margin="5" IsReadOnly="True"/>
            <Label Content="Full Name:" Grid.Row="0" Grid.Column="2" VerticalAlignment="Center"/>
            <TextBox x:Name="txtFullName" Grid.Row="0" Grid.Column="3" Margin="5"/>

            <Label Content="Phone:" Grid.Row="1" Grid.Column="0" VerticalAlignment="Center"/>
            <TextBox x:Name="txtPhone" Grid.Row="1" Grid.Column="1" Margin="5"/>
            <Label Content="Email:" Grid.Row="1" Grid.Column="2" VerticalAlignment="Center"/>
            <TextBox x:Name="txtEmail" Grid.Row="1" Grid.Column="3" Margin="5"/>

            <Label Content="Created At:" Grid.Row="2" Grid.Column="0" VerticalAlignment="Center"/>
            <TextBox x:Name="txtCreatedAt" Grid.Row="2" Grid.Column="1" Margin="5" IsReadOnly="True"/>
        </Grid>

        <!-- DataGrid for Customers -->
        <DataGrid x:Name="dgCustomers" Grid.Row="2" AutoGenerateColumns="False" 
                  SelectionChanged="dgCustomers_SelectionChanged" CanUserAddRows="False">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding Id}" IsReadOnly="True" Width="80"/>
                <DataGridTextColumn Header="Full Name" Binding="{Binding FullName}" Width="200"/>
                <DataGridTextColumn Header="Phone" Binding="{Binding Phone}" Width="150"/>
                <DataGridTextColumn Header="Email" Binding="{Binding Email}" Width="200"/>
                <DataGridTextColumn Header="Created At" Binding="{Binding CreatedAt, StringFormat={}{0:dd/MM/yyyy HH:mm}}" Width="*"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Window>